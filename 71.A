#include<bits/stdc++.h>
using namespace std;
int n;
string s;
int main()
{
//	freopen("data.in","r",stdin);
scanf("%d",&n);
for(int i=1;i<=n;i++)
{
	cin>>s;
	if(s.length()>10)
	printf("%c%d%c\n",s[0],s.length()-2,s[s.length()-1]);
	else
	cout<<s<<endl;
}
	return 0;

}
